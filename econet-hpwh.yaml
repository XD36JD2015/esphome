substitutions:
  tx_pin: GPIO6
  rx_pin: GPIO5
  name: econet-hpwh
  friendly_name: Water Heater

esp32:
  board: esp32-s3-devkitc-1
  variant: esp32s3
  framework:
    type: esp-idf

esphome:
  name: ${name}
  name_add_mac_suffix: false
  friendly_name: ${friendly_name}

ota:
  - platform: esphome
    password: !secret econet_hpwh_ota_password

api:
  encryption:
    key: !secret econet_hpwh_api_key

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  domain: .browncowalliance.io
  fast_connect: on

time:
  - platform: homeassistant
    id: homeassistant_time

packages:
  econet:
    url: https://github.com/esphome-econet/esphome-econet
    ref: main
    file: econet_heatpump_water_heater.yaml
    refresh: never
